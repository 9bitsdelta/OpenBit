{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Sandbox (debug)",
            "program": "${workspaceFolder}/bin/linux-x86_64-Debug/Sandbox",
            "request": "launch",
            "type": "cppdbg",
            "cwd": "${workspaceFolder}/Sandbox",
            "preLaunchTask": "Make all (debug)",
            "MIMode": "gdb"
        },
    ],
}

